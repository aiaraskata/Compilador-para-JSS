function int calcular(int a, int b) {
    let int resultado;
    resultado = a;
    return resultado;
}

function bool maiorDeIdade(int idade) {
    let bool resultado;
    return resultado;
}

function void main() {
    let int x;
    let int y;
    let real check;

    x = calcular(10, 20);
    check = maiorDeIdade(18);
}